{
    "name": "boat_signer_php",
    "version": "1.0.0",
    "description": "",
    "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1",
      "docker:build": "docker build -t exclusivelogin/boat-signer-back .",
      "docker:publish": "npm run docker:build && docker login && docker push exclusivelogin/boat-signer-back",
      "docker:run": "docker pull exclusivelogin/boat-signer-back && docker run --rm -d -p 8080:80 exclusivelogin/boat-signer-back"
    },
    "repository": {
      "type": "git",
      "url": "git+https://github.com/exclusiveLogin/boat-signer.git"
    },
    "author": "",
    "license": "ISC",
    "bugs": {
      "url": "https://github.com/exclusiveLogin/boat-signer/issues"
    },
    "homepage": "https://github.com/exclusiveLogin/boat-signer#readme"
  }
  